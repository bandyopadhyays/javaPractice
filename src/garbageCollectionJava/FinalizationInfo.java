package garbageCollectionJava;

public class FinalizationInfo {
	
	/**
	 * 
	 * Just before destroying an object garbage collector calls finalize() method to perform clean up activity.
	 * 
	 * once finalize method computes garbage collector destroys the object.
	 * 
	 * finalize meghod present on object class.	 
	 * 
	 * to make object eligible for garbage collection make it null.
	 * 
	 */

}
